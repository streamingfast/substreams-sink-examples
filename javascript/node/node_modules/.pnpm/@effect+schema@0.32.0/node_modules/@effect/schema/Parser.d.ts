/**
 * @since 1.0.0
 */
import * as E from "@effect/data/Either";
import type { Option } from "@effect/data/Option";
import * as Effect from "@effect/io/Effect";
import type { ParseOptions } from "@effect/schema/AST";
import type { ParseResult } from "@effect/schema/ParseResult";
import * as PR from "@effect/schema/ParseResult";
import type { Schema, To } from "@effect/schema/Schema";
/**
 * @category parsing
 * @since 1.0.0
 */
export declare const parseSync: <_, A>(schema: Schema<_, A>) => (i: unknown, options?: ParseOptions) => A;
/**
 * @category parsing
 * @since 1.0.0
 */
export declare const parseOption: <_, A>(schema: Schema<_, A>) => (i: unknown, options?: ParseOptions) => Option<A>;
/**
 * @category parsing
 * @since 1.0.0
 */
export declare const parseEither: <_, A>(schema: Schema<_, A>) => (i: unknown, options?: ParseOptions) => E.Either<PR.ParseError, A>;
/**
 * @category parsing
 * @since 1.0.0
 */
export declare const parseResult: <_, A>(schema: Schema<_, A>) => (i: unknown, options?: ParseOptions) => ParseResult<A>;
/**
 * @category parsing
 * @since 1.0.0
 */
export declare const parsePromise: <_, A>(schema: Schema<_, A>) => (i: unknown, options?: ParseOptions) => Promise<A>;
/**
 * @category parsing
 * @since 1.0.0
 */
export declare const parse: <_, A>(schema: Schema<_, A>) => (i: unknown, options?: ParseOptions) => Effect.Effect<never, PR.ParseError, A>;
/**
 * @category decoding
 * @since 1.0.0
 */
export declare const decodeSync: <I, A>(schema: Schema<I, A>) => (i: I, options?: ParseOptions) => A;
/**
 * @category decoding
 * @since 1.0.0
 */
export declare const decodeOption: <I, A>(schema: Schema<I, A>) => (i: I, options?: ParseOptions) => Option<A>;
/**
 * @category decoding
 * @since 1.0.0
 */
export declare const decodeEither: <I, A>(schema: Schema<I, A>) => (i: I, options?: ParseOptions) => E.Either<PR.ParseError, A>;
/**
 * @category decoding
 * @since 1.0.0
 */
export declare const decodeResult: <I, A>(schema: Schema<I, A>) => (i: I, options?: ParseOptions | undefined) => ParseResult<A>;
/**
 * @category decoding
 * @since 1.0.0
 */
export declare const decodePromise: <I, A>(schema: Schema<I, A>) => (i: I, options?: ParseOptions) => Promise<A>;
/**
 * @category decoding
 * @since 1.0.0
 */
export declare const decode: <I, A>(schema: Schema<I, A>) => (i: I, options?: ParseOptions | undefined) => Effect.Effect<never, PR.ParseError, A>;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const validateSync: <_, A>(schema: Schema<_, A>) => (a: unknown, options?: ParseOptions) => A;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const validateOption: <_, A>(schema: Schema<_, A>) => (a: unknown, options?: ParseOptions) => Option<A>;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const validateEither: <_, A>(schema: Schema<_, A>) => (a: unknown, options?: ParseOptions) => E.Either<PR.ParseError, A>;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const validateResult: <_, A>(schema: Schema<_, A>) => (a: unknown, options?: ParseOptions) => ParseResult<A>;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const validatePromise: <_, A>(schema: Schema<_, A>) => (i: unknown, options?: ParseOptions) => Promise<A>;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const validate: <_, A>(schema: Schema<_, A>) => (a: unknown, options?: ParseOptions) => Effect.Effect<never, PR.ParseError, A>;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const is: <_, A>(schema: Schema<_, A>) => (a: unknown) => a is A;
/**
 * @since 1.0.0
 */
export type ToAsserts<S extends Schema<any>> = (input: unknown, options?: ParseOptions) => asserts input is To<S>;
/**
 * @category validation
 * @since 1.0.0
 */
export declare const asserts: <_, A>(schema: Schema<_, A>) => (a: unknown, options?: ParseOptions) => asserts a is A;
/**
 * @category encoding
 * @since 1.0.0
 */
export declare const encodeSync: <I, A>(schema: Schema<I, A>) => (a: A, options?: ParseOptions) => I;
/**
 * @category encoding
 * @since 1.0.0
 */
export declare const encodeOption: <I, A>(schema: Schema<I, A>) => (input: A, options?: ParseOptions) => Option<I>;
/**
 * @category encoding
 * @since 1.0.0
 */
export declare const encodeEither: <I, A>(schema: Schema<I, A>) => (a: A, options?: ParseOptions) => E.Either<PR.ParseError, I>;
/**
 * @category encoding
 * @since 1.0.0
 */
export declare const encodeResult: <I, A>(schema: Schema<I, A>) => (a: A, options?: ParseOptions) => ParseResult<I>;
/**
 * @category encoding
 * @since 1.0.0
 */
export declare const encodePromise: <I, A>(schema: Schema<I, A>) => (a: A, options?: ParseOptions) => Promise<I>;
/**
 * @category encoding
 * @since 1.0.0
 */
export declare const encode: <I, A>(schema: Schema<I, A>) => (a: A, options?: ParseOptions) => Effect.Effect<never, PR.ParseError, I>;
//# sourceMappingURL=Parser.d.ts.map