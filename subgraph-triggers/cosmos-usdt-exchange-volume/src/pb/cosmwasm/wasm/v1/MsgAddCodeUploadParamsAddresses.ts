// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class MsgAddCodeUploadParamsAddresses {
  static encode(
    message: MsgAddCodeUploadParamsAddresses,
    writer: Writer
  ): void {
    writer.uint32(10);
    writer.string(message.authority);

    const addresses = message.addresses;
    if (addresses.length !== 0) {
      for (let i: i32 = 0; i < addresses.length; ++i) {
        writer.uint32(18);
        writer.string(addresses[i]);
      }
    }
  }

  static decode(reader: Reader, length: i32): MsgAddCodeUploadParamsAddresses {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgAddCodeUploadParamsAddresses();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.authority = reader.string();
          break;

        case 2:
          message.addresses.push(reader.string());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  authority: string;
  addresses: Array<string>;

  constructor(authority: string = "", addresses: Array<string> = []) {
    this.authority = authority;
    this.addresses = addresses;
  }
}
