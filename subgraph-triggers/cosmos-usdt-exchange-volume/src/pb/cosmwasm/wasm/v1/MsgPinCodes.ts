// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class MsgPinCodes {
  static encode(message: MsgPinCodes, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.authority);

    const codeIds = message.codeIds;
    if (codeIds.length !== 0) {
      for (let i: i32 = 0; i < codeIds.length; ++i) {
        writer.uint32(16);
        writer.uint64(codeIds[i]);
      }
    }
  }

  static decode(reader: Reader, length: i32): MsgPinCodes {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgPinCodes();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.authority = reader.string();
          break;

        case 2:
          message.codeIds.push(reader.uint64());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  authority: string;
  codeIds: Array<u64>;

  constructor(authority: string = "", codeIds: Array<u64> = []) {
    this.authority = authority;
    this.codeIds = codeIds;
  }
}
