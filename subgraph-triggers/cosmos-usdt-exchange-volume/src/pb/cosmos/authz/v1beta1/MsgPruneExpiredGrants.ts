// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class MsgPruneExpiredGrants {
  static encode(message: MsgPruneExpiredGrants, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.pruner);
  }

  static decode(reader: Reader, length: i32): MsgPruneExpiredGrants {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgPruneExpiredGrants();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.pruner = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  pruner: string;

  constructor(pruner: string = "") {
    this.pruner = pruner;
  }
}
