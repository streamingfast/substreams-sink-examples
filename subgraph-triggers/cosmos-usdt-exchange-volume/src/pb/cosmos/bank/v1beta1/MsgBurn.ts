// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { Coin } from "../../base/v1beta1/Coin";

export class MsgBurn {
  static encode(message: MsgBurn, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.fromAddress);

    const amount = message.amount;
    for (let i: i32 = 0; i < amount.length; ++i) {
      writer.uint32(18);
      writer.fork();
      Coin.encode(amount[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): MsgBurn {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgBurn();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.fromAddress = reader.string();
          break;

        case 2:
          message.amount.push(Coin.decode(reader, reader.uint32()));
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  fromAddress: string;
  amount: Array<Coin>;

  constructor(fromAddress: string = "", amount: Array<Coin> = []) {
    this.fromAddress = fromAddress;
    this.amount = amount;
  }
}
