// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { DecCoin } from "../../base/v1beta1/DecCoin";

export class ValidatorOutstandingRewards {
  static encode(message: ValidatorOutstandingRewards, writer: Writer): void {
    const rewards = message.rewards;
    for (let i: i32 = 0; i < rewards.length; ++i) {
      writer.uint32(10);
      writer.fork();
      DecCoin.encode(rewards[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): ValidatorOutstandingRewards {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new ValidatorOutstandingRewards();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.rewards.push(DecCoin.decode(reader, reader.uint32()));
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  rewards: Array<DecCoin>;

  constructor(rewards: Array<DecCoin> = []) {
    this.rewards = rewards;
  }
}
