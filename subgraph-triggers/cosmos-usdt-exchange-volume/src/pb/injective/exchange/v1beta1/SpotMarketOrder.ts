// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { OrderInfo } from "./OrderInfo";
import { OrderType } from "./OrderType";

export class SpotMarketOrder {
  static encode(message: SpotMarketOrder, writer: Writer): void {
    const orderInfo = message.orderInfo;
    if (orderInfo !== null) {
      writer.uint32(10);
      writer.fork();
      OrderInfo.encode(orderInfo, writer);
      writer.ldelim();
    }

    writer.uint32(18);
    writer.string(message.balanceHold);

    writer.uint32(26);
    writer.bytes(message.orderHash);

    writer.uint32(32);
    writer.int32(message.orderType);

    writer.uint32(42);
    writer.string(message.triggerPrice);
  }

  static decode(reader: Reader, length: i32): SpotMarketOrder {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new SpotMarketOrder();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.orderInfo = OrderInfo.decode(reader, reader.uint32());
          break;

        case 2:
          message.balanceHold = reader.string();
          break;

        case 3:
          message.orderHash = reader.bytes();
          break;

        case 4:
          message.orderType = reader.int32();
          break;

        case 5:
          message.triggerPrice = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  orderInfo: OrderInfo | null;
  balanceHold: string;
  orderHash: Uint8Array;
  orderType: OrderType;
  triggerPrice: string;

  constructor(
    orderInfo: OrderInfo | null = null,
    balanceHold: string = "",
    orderHash: Uint8Array = new Uint8Array(0),
    orderType: OrderType = 0,
    triggerPrice: string = ""
  ) {
    this.orderInfo = orderInfo;
    this.balanceHold = balanceHold;
    this.orderHash = orderHash;
    this.orderType = orderType;
    this.triggerPrice = triggerPrice;
  }
}
