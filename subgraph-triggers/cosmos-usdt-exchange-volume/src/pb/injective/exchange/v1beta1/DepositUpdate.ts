// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { SubaccountDeposit } from "./SubaccountDeposit";

export class DepositUpdate {
  static encode(message: DepositUpdate, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.denom);

    const deposits = message.deposits;
    for (let i: i32 = 0; i < deposits.length; ++i) {
      writer.uint32(18);
      writer.fork();
      SubaccountDeposit.encode(deposits[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): DepositUpdate {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new DepositUpdate();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.denom = reader.string();
          break;

        case 2:
          message.deposits.push(
            SubaccountDeposit.decode(reader, reader.uint32())
          );
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  denom: string;
  deposits: Array<SubaccountDeposit>;

  constructor(denom: string = "", deposits: Array<SubaccountDeposit> = []) {
    this.denom = denom;
    this.deposits = deposits;
  }
}
