// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class SubaccountIDs {
  static encode(message: SubaccountIDs, writer: Writer): void {
    const subaccountIds = message.subaccountIds;
    if (subaccountIds.length !== 0) {
      for (let i: i32 = 0; i < subaccountIds.length; ++i) {
        writer.uint32(10);
        writer.bytes(subaccountIds[i]);
      }
    }
  }

  static decode(reader: Reader, length: i32): SubaccountIDs {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new SubaccountIDs();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.subaccountIds.push(reader.bytes());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  subaccountIds: Array<Uint8Array>;

  constructor(subaccountIds: Array<Uint8Array> = []) {
    this.subaccountIds = subaccountIds;
  }
}
