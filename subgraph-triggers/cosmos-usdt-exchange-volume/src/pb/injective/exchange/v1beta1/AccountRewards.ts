// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { Coin } from "../../../cosmos/base/v1beta1/Coin";

export class AccountRewards {
  static encode(message: AccountRewards, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.account);

    const rewards = message.rewards;
    for (let i: i32 = 0; i < rewards.length; ++i) {
      writer.uint32(18);
      writer.fork();
      Coin.encode(rewards[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): AccountRewards {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new AccountRewards();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.account = reader.string();
          break;

        case 2:
          message.rewards.push(Coin.decode(reader, reader.uint32()));
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  account: string;
  rewards: Array<Coin>;

  constructor(account: string = "", rewards: Array<Coin> = []) {
    this.account = account;
    this.rewards = rewards;
  }
}
