// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { OrderData } from "./OrderData";

export class MsgBatchCancelSpotOrders {
  static encode(message: MsgBatchCancelSpotOrders, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.sender);

    const data = message.data;
    for (let i: i32 = 0; i < data.length; ++i) {
      writer.uint32(18);
      writer.fork();
      OrderData.encode(data[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): MsgBatchCancelSpotOrders {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgBatchCancelSpotOrders();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.sender = reader.string();
          break;

        case 2:
          message.data.push(OrderData.decode(reader, reader.uint32()));
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  sender: string;
  data: Array<OrderData>;

  constructor(sender: string = "", data: Array<OrderData> = []) {
    this.sender = sender;
    this.data = data;
  }
}
