// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class Level {
  static encode(message: Level, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.p);

    writer.uint32(18);
    writer.string(message.q);
  }

  static decode(reader: Reader, length: i32): Level {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new Level();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.p = reader.string();
          break;

        case 2:
          message.q = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  p: string;
  q: string;

  constructor(p: string = "", q: string = "") {
    this.p = p;
    this.q = q;
  }
}
