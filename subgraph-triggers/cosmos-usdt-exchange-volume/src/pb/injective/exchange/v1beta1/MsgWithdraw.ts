// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { Coin } from "../../../cosmos/base/v1beta1/Coin";

export class MsgWithdraw {
  static encode(message: MsgWithdraw, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.sender);

    writer.uint32(18);
    writer.string(message.subaccountId);

    const amount = message.amount;
    if (amount !== null) {
      writer.uint32(26);
      writer.fork();
      Coin.encode(amount, writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): MsgWithdraw {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgWithdraw();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.sender = reader.string();
          break;

        case 2:
          message.subaccountId = reader.string();
          break;

        case 3:
          message.amount = Coin.decode(reader, reader.uint32());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  sender: string;
  subaccountId: string;
  amount: Coin | null;

  constructor(
    sender: string = "",
    subaccountId: string = "",
    amount: Coin | null = null
  ) {
    this.sender = sender;
    this.subaccountId = subaccountId;
    this.amount = amount;
  }
}
