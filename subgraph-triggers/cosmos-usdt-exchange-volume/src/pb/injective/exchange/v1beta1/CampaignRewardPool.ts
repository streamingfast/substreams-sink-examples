// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { Coin } from "../../../cosmos/base/v1beta1/Coin";

export class CampaignRewardPool {
  static encode(message: CampaignRewardPool, writer: Writer): void {
    writer.uint32(8);
    writer.int64(message.startTimestamp);

    const maxCampaignRewards = message.maxCampaignRewards;
    for (let i: i32 = 0; i < maxCampaignRewards.length; ++i) {
      writer.uint32(18);
      writer.fork();
      Coin.encode(maxCampaignRewards[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): CampaignRewardPool {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new CampaignRewardPool();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.startTimestamp = reader.int64();
          break;

        case 2:
          message.maxCampaignRewards.push(Coin.decode(reader, reader.uint32()));
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  startTimestamp: i64;
  maxCampaignRewards: Array<Coin>;

  constructor(startTimestamp: i64 = 0, maxCampaignRewards: Array<Coin> = []) {
    this.startTimestamp = startTimestamp;
    this.maxCampaignRewards = maxCampaignRewards;
  }
}
