// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class BridgeValidator {
  static encode(message: BridgeValidator, writer: Writer): void {
    writer.uint32(8);
    writer.uint64(message.power);

    writer.uint32(18);
    writer.string(message.ethereumAddress);
  }

  static decode(reader: Reader, length: i32): BridgeValidator {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new BridgeValidator();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.power = reader.uint64();
          break;

        case 2:
          message.ethereumAddress = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  power: u64;
  ethereumAddress: string;

  constructor(power: u64 = 0, ethereumAddress: string = "") {
    this.power = power;
    this.ethereumAddress = ethereumAddress;
  }
}
