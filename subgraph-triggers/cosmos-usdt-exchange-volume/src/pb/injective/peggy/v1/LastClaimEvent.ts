// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class LastClaimEvent {
  static encode(message: LastClaimEvent, writer: Writer): void {
    writer.uint32(8);
    writer.uint64(message.ethereumEventNonce);

    writer.uint32(16);
    writer.uint64(message.ethereumEventHeight);
  }

  static decode(reader: Reader, length: i32): LastClaimEvent {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new LastClaimEvent();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.ethereumEventNonce = reader.uint64();
          break;

        case 2:
          message.ethereumEventHeight = reader.uint64();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  ethereumEventNonce: u64;
  ethereumEventHeight: u64;

  constructor(ethereumEventNonce: u64 = 0, ethereumEventHeight: u64 = 0) {
    this.ethereumEventNonce = ethereumEventNonce;
    this.ethereumEventHeight = ethereumEventHeight;
  }
}
