// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class MsgRequestBatch {
  static encode(message: MsgRequestBatch, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.orchestrator);

    writer.uint32(18);
    writer.string(message.denom);
  }

  static decode(reader: Reader, length: i32): MsgRequestBatch {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgRequestBatch();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.orchestrator = reader.string();
          break;

        case 2:
          message.denom = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  orchestrator: string;
  denom: string;

  constructor(orchestrator: string = "", denom: string = "") {
    this.orchestrator = orchestrator;
    this.denom = denom;
  }
}
