// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { PriceState } from "./PriceState";

export class PythPriceState {
  static encode(message: PythPriceState, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.priceId);

    writer.uint32(18);
    writer.string(message.emaPrice);

    writer.uint32(26);
    writer.string(message.emaConf);

    writer.uint32(34);
    writer.string(message.conf);

    writer.uint32(40);
    writer.uint64(message.publishTime);

    const priceState = message.priceState;
    if (priceState !== null) {
      writer.uint32(50);
      writer.fork();
      PriceState.encode(priceState, writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): PythPriceState {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new PythPriceState();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.priceId = reader.string();
          break;

        case 2:
          message.emaPrice = reader.string();
          break;

        case 3:
          message.emaConf = reader.string();
          break;

        case 4:
          message.conf = reader.string();
          break;

        case 5:
          message.publishTime = reader.uint64();
          break;

        case 6:
          message.priceState = PriceState.decode(reader, reader.uint32());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  priceId: string;
  emaPrice: string;
  emaConf: string;
  conf: string;
  publishTime: u64;
  priceState: PriceState | null;

  constructor(
    priceId: string = "",
    emaPrice: string = "",
    emaConf: string = "",
    conf: string = "",
    publishTime: u64 = 0,
    priceState: PriceState | null = null
  ) {
    this.priceId = priceId;
    this.emaPrice = emaPrice;
    this.emaConf = emaConf;
    this.conf = conf;
    this.publishTime = publishTime;
    this.priceState = priceState;
  }
}
