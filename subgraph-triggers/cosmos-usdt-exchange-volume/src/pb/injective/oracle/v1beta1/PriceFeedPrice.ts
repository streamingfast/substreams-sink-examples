// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class PriceFeedPrice {
  static encode(message: PriceFeedPrice, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.price);
  }

  static decode(reader: Reader, length: i32): PriceFeedPrice {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new PriceFeedPrice();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.price = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  price: string;

  constructor(price: string = "") {
    this.price = price;
  }
}
