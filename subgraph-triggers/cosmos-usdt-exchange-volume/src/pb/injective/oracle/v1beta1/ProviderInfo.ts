// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class ProviderInfo {
  static encode(message: ProviderInfo, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.provider);

    const relayers = message.relayers;
    if (relayers.length !== 0) {
      for (let i: i32 = 0; i < relayers.length; ++i) {
        writer.uint32(18);
        writer.string(relayers[i]);
      }
    }
  }

  static decode(reader: Reader, length: i32): ProviderInfo {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new ProviderInfo();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.provider = reader.string();
          break;

        case 2:
          message.relayers.push(reader.string());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  provider: string;
  relayers: Array<string>;

  constructor(provider: string = "", relayers: Array<string> = []) {
    this.provider = provider;
    this.relayers = relayers;
  }
}
