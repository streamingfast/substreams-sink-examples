// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { SymbolPriceTimestamp } from "./SymbolPriceTimestamp";

export class LastPriceTimestamps {
  static encode(message: LastPriceTimestamps, writer: Writer): void {
    const lastPriceTimestamps = message.lastPriceTimestamps;
    for (let i: i32 = 0; i < lastPriceTimestamps.length; ++i) {
      writer.uint32(10);
      writer.fork();
      SymbolPriceTimestamp.encode(lastPriceTimestamps[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): LastPriceTimestamps {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new LastPriceTimestamps();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.lastPriceTimestamps.push(
            SymbolPriceTimestamp.decode(reader, reader.uint32())
          );
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  lastPriceTimestamps: Array<SymbolPriceTimestamp>;

  constructor(lastPriceTimestamps: Array<SymbolPriceTimestamp> = []) {
    this.lastPriceTimestamps = lastPriceTimestamps;
  }
}
