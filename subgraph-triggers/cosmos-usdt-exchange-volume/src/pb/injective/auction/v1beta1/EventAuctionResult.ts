// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class EventAuctionResult {
  static encode(message: EventAuctionResult, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.winner);

    writer.uint32(18);
    writer.string(message.amount);

    writer.uint32(24);
    writer.uint64(message.round);
  }

  static decode(reader: Reader, length: i32): EventAuctionResult {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new EventAuctionResult();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.winner = reader.string();
          break;

        case 2:
          message.amount = reader.string();
          break;

        case 3:
          message.round = reader.uint64();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  winner: string;
  amount: string;
  round: u64;

  constructor(winner: string = "", amount: string = "", round: u64 = 0) {
    this.winner = winner;
    this.amount = amount;
    this.round = round;
  }
}
