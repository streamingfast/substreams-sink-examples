// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { Coin } from "../../../cosmos/base/v1beta1/Coin";

export class MsgBid {
  static encode(message: MsgBid, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.sender);

    const bidAmount = message.bidAmount;
    if (bidAmount !== null) {
      writer.uint32(18);
      writer.fork();
      Coin.encode(bidAmount, writer);
      writer.ldelim();
    }

    writer.uint32(24);
    writer.uint64(message.round);
  }

  static decode(reader: Reader, length: i32): MsgBid {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new MsgBid();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.sender = reader.string();
          break;

        case 2:
          message.bidAmount = Coin.decode(reader, reader.uint32());
          break;

        case 3:
          message.round = reader.uint64();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  sender: string;
  bidAmount: Coin | null;
  round: u64;

  constructor(
    sender: string = "",
    bidAmount: Coin | null = null,
    round: u64 = 0
  ) {
    this.sender = sender;
    this.bidAmount = bidAmount;
    this.round = round;
  }
}
