// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class ModuleMetadata {
  static encode(message: ModuleMetadata, writer: Writer): void {
    writer.uint32(8);
    writer.uint64(message.packageIndex);

    writer.uint32(18);
    writer.string(message.doc);
  }

  static decode(reader: Reader, length: i32): ModuleMetadata {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new ModuleMetadata();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.packageIndex = reader.uint64();
          break;

        case 2:
          message.doc = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  packageIndex: u64;
  doc: string;

  constructor(packageIndex: u64 = 0, doc: string = "") {
    this.packageIndex = packageIndex;
    this.doc = doc;
  }
}
