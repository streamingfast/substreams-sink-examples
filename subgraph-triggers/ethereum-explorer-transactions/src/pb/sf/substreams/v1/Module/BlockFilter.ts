// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class BlockFilter {
  static encode(message: BlockFilter, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.module);

    writer.uint32(18);
    writer.string(message.query);
  }

  static decode(reader: Reader, length: i32): BlockFilter {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new BlockFilter();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.module = reader.string();
          break;

        case 2:
          message.query = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  module: string;
  query: string;

  constructor(module: string = "", query: string = "") {
    this.module = module;
    this.query = query;
  }
}
