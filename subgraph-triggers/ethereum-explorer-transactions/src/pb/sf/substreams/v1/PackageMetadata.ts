// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class PackageMetadata {
  static encode(message: PackageMetadata, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.version);

    writer.uint32(18);
    writer.string(message.url);

    writer.uint32(26);
    writer.string(message.name);

    writer.uint32(34);
    writer.string(message.doc);
  }

  static decode(reader: Reader, length: i32): PackageMetadata {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new PackageMetadata();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.version = reader.string();
          break;

        case 2:
          message.url = reader.string();
          break;

        case 3:
          message.name = reader.string();
          break;

        case 4:
          message.doc = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  version: string;
  url: string;
  name: string;
  doc: string;

  constructor(
    version: string = "",
    url: string = "",
    name: string = "",
    doc: string = ""
  ) {
    this.version = version;
    this.url = url;
    this.name = name;
    this.doc = doc;
  }
}
