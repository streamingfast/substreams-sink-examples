// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class PackageInfo {
  static encode(message: PackageInfo, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.name);

    writer.uint32(18);
    writer.string(message.version);

    writer.uint32(26);
    writer.string(message.outputModuleName);

    writer.uint32(34);
    writer.string(message.outputModuleHash);
  }

  static decode(reader: Reader, length: i32): PackageInfo {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new PackageInfo();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.name = reader.string();
          break;

        case 2:
          message.version = reader.string();
          break;

        case 3:
          message.outputModuleName = reader.string();
          break;

        case 4:
          message.outputModuleHash = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  name: string;
  version: string;
  outputModuleName: string;
  outputModuleHash: string;

  constructor(
    name: string = "",
    version: string = "",
    outputModuleName: string = "",
    outputModuleHash: string = ""
  ) {
    this.name = name;
    this.version = version;
    this.outputModuleName = outputModuleName;
    this.outputModuleHash = outputModuleHash;
  }
}
