// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { StoreDelta } from "./StoreDelta";
import { OutputDebugInfo } from "./OutputDebugInfo";

export class StoreModuleOutput {
  static encode(message: StoreModuleOutput, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.name);

    const debugStoreDeltas = message.debugStoreDeltas;
    for (let i: i32 = 0; i < debugStoreDeltas.length; ++i) {
      writer.uint32(18);
      writer.fork();
      StoreDelta.encode(debugStoreDeltas[i], writer);
      writer.ldelim();
    }

    const debugInfo = message.debugInfo;
    if (debugInfo !== null) {
      writer.uint32(82);
      writer.fork();
      OutputDebugInfo.encode(debugInfo, writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): StoreModuleOutput {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new StoreModuleOutput();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.name = reader.string();
          break;

        case 2:
          message.debugStoreDeltas.push(
            StoreDelta.decode(reader, reader.uint32())
          );
          break;

        case 10:
          message.debugInfo = OutputDebugInfo.decode(reader, reader.uint32());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  name: string;
  debugStoreDeltas: Array<StoreDelta>;
  debugInfo: OutputDebugInfo | null;

  constructor(
    name: string = "",
    debugStoreDeltas: Array<StoreDelta> = [],
    debugInfo: OutputDebugInfo | null = null
  ) {
    this.name = name;
    this.debugStoreDeltas = debugStoreDeltas;
    this.debugInfo = debugInfo;
  }
}
