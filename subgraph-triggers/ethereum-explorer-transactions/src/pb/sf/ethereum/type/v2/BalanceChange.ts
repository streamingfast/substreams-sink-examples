// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { BigInt } from "./BigInt";
import { Reason } from "./BalanceChange/Reason";

export class BalanceChange {
  static encode(message: BalanceChange, writer: Writer): void {
    writer.uint32(10);
    writer.bytes(message.address);

    const oldValue = message.oldValue;
    if (oldValue !== null) {
      writer.uint32(18);
      writer.fork();
      BigInt.encode(oldValue, writer);
      writer.ldelim();
    }

    const newValue = message.newValue;
    if (newValue !== null) {
      writer.uint32(26);
      writer.fork();
      BigInt.encode(newValue, writer);
      writer.ldelim();
    }

    writer.uint32(32);
    writer.int32(message.reason);

    writer.uint32(40);
    writer.uint64(message.ordinal);
  }

  static decode(reader: Reader, length: i32): BalanceChange {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new BalanceChange();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.address = reader.bytes();
          break;

        case 2:
          message.oldValue = BigInt.decode(reader, reader.uint32());
          break;

        case 3:
          message.newValue = BigInt.decode(reader, reader.uint32());
          break;

        case 4:
          message.reason = reader.int32();
          break;

        case 5:
          message.ordinal = reader.uint64();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  address: Uint8Array;
  oldValue: BigInt | null;
  newValue: BigInt | null;
  reason: Reason;
  ordinal: u64;

  constructor(
    address: Uint8Array = new Uint8Array(0),
    oldValue: BigInt | null = null,
    newValue: BigInt | null = null,
    reason: Reason = 0,
    ordinal: u64 = 0
  ) {
    this.address = address;
    this.oldValue = oldValue;
    this.newValue = newValue;
    this.reason = reason;
    this.ordinal = ordinal;
  }
}
