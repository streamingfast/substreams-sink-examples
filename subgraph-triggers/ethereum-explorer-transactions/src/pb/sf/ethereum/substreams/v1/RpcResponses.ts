// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";
import { RpcResponse } from "./RpcResponse";

export class RpcResponses {
  static encode(message: RpcResponses, writer: Writer): void {
    const responses = message.responses;
    for (let i: i32 = 0; i < responses.length; ++i) {
      writer.uint32(10);
      writer.fork();
      RpcResponse.encode(responses[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): RpcResponses {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new RpcResponses();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(RpcResponse.decode(reader, reader.uint32()));
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  responses: Array<RpcResponse>;

  constructor(responses: Array<RpcResponse> = []) {
    this.responses = responses;
  }
}
